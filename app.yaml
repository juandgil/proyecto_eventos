runtime: nodejs14

env_variables:
  DATABASE_URL: "postgres://usuario:contraseña@/nombre_db?host=/cloudsql/INSTANCE_CONNECTION_NAME"

beta_settings:
  cloud_sql_instances: "INSTANCE_CONNECTION_NAME"
